打开 **PowerShell** 后，直接粘贴下面这行命令（把里面的邮箱**换成你自己的 GitHub 邮箱**）：

powershell

```
ssh-keygen -t ed25519 -C "你的GitHub邮箱"
```

然后**连续按 3 次回车**，什么都不用输入。

---

# 第二步：复制公钥（直接复制运行）

继续在 PowerShell 里输入：

powershell

```
cat ~/.ssh/id_ed25519.pub | clip
```

✅ **运行完，公钥已经自动复制到剪贴板了！**

---

# 第三步：去 GitHub 粘贴（网页操作）

1. 打开 GitHub → 点右上角头像 → **Settings**
2. 左边点 **SSH and GPG keys**
3. 点右上角 **New SSH key**
4. **Title** 随便写：比如 `我的笔记本`
5. **Key** 框里 **直接粘贴**（刚才已经自动复制好了）
6. 点 **Add SSH key**

完成！

---

# 第四步：回到 PowerShell，测试连接

输入：

powershell

```
ssh -T git@github.com
```

如果弹出提示，输入 **yes** 再回车。

出现这句话就是**成功了**：

plaintext

```
Hi xxx! You've successfully authenticated...
```
![669](assets/2026-04-07%20配置SSH协议/file-20260407004525889.jpg)
---

# 第五步：把你的仓库改成 SSH 协议

进入你的笔记仓库（把路径换成你自己的）：

powershell

```
cd D:\obsidian_vault
```

然后运行修改命令：

powershell

```
git remote set-url origin git@github.com:3021604392-source/obsidian_vault.git
```

---

# 最后：推送到 GitHub

powershell

```
git push
```

✅ **成功！再也不会出现连接重置错误了。**

