这个报错 `Recv failure: Connection was reset` 是典型的**网络连接问题**，不是你的 Git 仓库或代码本身的错误。
![](assets/更换%20Git%20远程地址协议%202026-04-07/file-20260407011314955.jpg)
当前你用的是 HTTPS 协议，这种协议在国内网络环境下很容易被干扰，换成 SSH 协议会稳定很多

## 打开 **PowerShell** 
直接粘贴下面这行命令（把里面的邮箱**换成你自己的 GitHub 邮箱**）：

powershell

```
ssh-keygen -t ed25519 -C "你的GitHub邮箱"
```

然后**连续按 3 次回车**，什么都不用输入。

---

# 第二步：复制公钥（直接复制运行）

继续在 PowerShell 里输入：

powershell

```
cat ~/.ssh/id_ed25519.pub | clip
```

✅ **运行完，公钥已经自动复制到剪贴板了！**

---

# 第三步：去 GitHub 粘贴（网页操作）

1. 打开 GitHub → 点右上角头像 → **Settings**
2. 左边点 **SSH and GPG keys**
3. 点右上角 **New SSH key**
4. **Title** 随便写：比如 `我的笔记本`
5. **Key** 框里 **直接粘贴**（刚才已经自动复制好了）
6. 点 **Add SSH key**

完成！

---

# 第四步：回到 PowerShell，测试连接

输入：

powershell

```
ssh -T git@github.com
```

如果弹出提示，输入 **yes** 再回车。

出现这句话就是**成功了**：

plaintext

```
Hi xxx! You've successfully authenticated...
```
![669](assets/2026-04-07%20配置SSH协议/file-20260407004525889.jpg)
---

# 第五步：把你的仓库改成 SSH 协议

进入你的笔记仓库（把路径换成你自己的）：

powershell

```
cd D:\obsidian_vault
```

然后运行修改命令：

powershell

```
git remote set-url origin git@github.com:3021604392-source/obsidian_vault.git
```

---

# 最后：推送到 GitHub

powershell

```
git push
```

✅ **成功！再也不会出现连接重置错误了。**

## 在 GitHub Desktop 里修改（推荐，一步到位）

这是最稳妥的方式，直接在你正在用的软件里改，不用装任何东西。

1. 打开 **GitHub Desktop**，切换到你的 `obsidian_vault` 仓库
2. 点击顶部菜单栏：`Repository` → `Repository Settings...`
3. 在弹出的窗口里，找到左侧的 **Remote** 选项卡
4. 把 `Primary remote repository (origin)` 里的地址，替换成你的 SSH 地址：
    
    plaintext
    
    ```
    git@github.com:3021604392-source/obsidian_vault.git
    ```
    
5. 点击 **Save** 保存，关闭窗口即可。

✅ 这样 GitHub Desktop 就会自动用 SSH 协议连接了，以后的 Push/Pull 都不会再出现连接重置的问题。

---

## 由于未安装git，powershell无法识别git环境，需从第五步开始实施方案二：在 Obsidian Git 插件里修改（双保险）

如果你还在用 Obsidian Git 插件自动同步，可以在插件里也设置一遍，确保两边都生效：

1. 打开 Obsidian → 进入 **设置**（左下角齿轮图标）
2. 在左侧菜单找到 **社区插件** → 点击 **Git**（你之前安装的那个）
3. 往下滑找到 **Remote settings**（远程设置）
4. 把 `Remote URL` 里的地址，替换成和上面一样的 SSH 地址：
    
    plaintext
    
    ```
    git@github.com:3021604392-source/obsidian_vault.git
    ```
    
5. 点击保存，重启 Obsidian 就生效了。

---

## 关键前提：确保你的 SSH Key 已经配置好

你之前已经在 GitHub 网页端添加了公钥，只要这个步骤完成，修改完地址后就可以直接用了：

- 打开 GitHub Desktop，尝试一次 Push 操作
- 或者在 Obsidian 里，用 Git 插件手动执行一次 `Pull`/`Push`
    
    如果没有报错，就说明已经切换成功了！